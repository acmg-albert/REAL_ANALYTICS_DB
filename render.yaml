services:
  - type: web
    name: real-analytics-db
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.scripts.scheduler
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
    autoDeploy: true 